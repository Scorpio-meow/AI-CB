# Enforce LF line endings for specific files to avoid CRLF normalization warnings on Windows
backend/requirements.txt text eol=lf

# You can add other files or patterns below if needed
# *.sh text eol=lf