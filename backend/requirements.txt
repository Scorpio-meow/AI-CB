# Core FastAPI dependencies
fastapi==0.104.1
uvicorn==0.24.0
pydantic>=2.7.0

# Windows Production Server
waitress>=3.0.0

# Database
sqlalchemy==2.0.23

# Configuration
python-dotenv==1.0.0

# LangChain core components  
langchain-core>=0.3.72,<1.0.0
langchain>=0.3.20,<0.4.0
langchain-community>=0.3.0,<0.4.0

# ML/AI dependencies with CUDA support
sentence-transformers==2.2.2
transformers==4.36.0
torch>=2.5.0  # CUDA 12.1+ 版本，從 PyTorch 官方源安裝
numpy==1.24.3
faiss-cpu==1.7.4  # 可選：改用 faiss-gpu 如有 CUDA 需求
scikit-learn==1.3.2

# Communication
websockets==12.0
requests>=2.32.0  # 升級以修復安全漏洞
httpx==0.25.0

# Document processing - 升級到更安全的版本
pypdf>=3.17.0  # 替換 PyPDF2，修復已知漏洞
python-docx==1.1.0

# Search and indexing
whoosh==2.7.4
jieba==0.42.1

# Utilities
filelock==3.12.2
huggingface_hub==0.19.3

# Security utilities
python-multipart>=0.0.6  # 檔案上傳安全
python-jose[cryptography]==3.3.0  # JWT 處理
passlib[bcrypt]==1.7.4  # 密碼加密
bcrypt==4.0.1  # 固定版本以相容 passlib 1.7.4
email-validator>=2.0.0  # Email 驗證 (Pydantic EmailStr)

# Advanced Security & Caching
cryptography>=41.0.0  # RSA 非對稱加密
redis>=5.0.0  # Token 黑名單快取 & RAG 查詢快取
aioredis>=2.0.1  # 異步 Redis 客戶端

# Testing
pytest-asyncio>=0.23.0
PyJWT>=2.9.0

